<section class="liftd-section liftd-section--ink liftd-hero">
  <div class="liftd-wrapper liftd-empty">
    <h1 class="liftd-title">{{ 'general.404.title' | t }}</h1>
    <p class="liftd-lead">{{ 'general.404.subtext' | t }}</p>
    <a href="{{ routes.all_products_collection_url }}" class="liftd-btn liftd-btn--pink">
      {{ 'general.404.link' | t }}
    </a>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
